---
async function query(data: { inputs: { question: string; context: string; }; }) {
	const response = await fetch(
		"https://api-inference.huggingface.co/models/deepset/roberta-base-squad2",
		{
			headers: { Authorization: "Bearer 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" },
			method: "POST",
			body: JSON.stringify(data),
		}
	);
	const result = await response.json();
	return result;
}

query({"inputs": {
		"question": "Projector is not showing anything",
		"context": "If a touch panel is unresponsive, reset the system."
	}}).then((response) => {
	console.log(JSON.stringify(response));
});
---